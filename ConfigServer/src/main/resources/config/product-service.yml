server:
  port: 8082
  wait-time-in-ms-when-sync-empty: 3000

spring:

  mongodb:
    uri: mongodb://product_user:product_password@localhost:27017/product_db?authSource=product_db
    auto-index-creation: true

  data:
    mongodb:
      uri: mongodb://product_user:product_password@localhost:27017/product_db?authSource=product_db
app:
  version: "1.0"